apiVersion: v1
kind: Service

metadata:
  name: dell-backend-service
  namespace: boothflow

spec:
  selector:
    app: dell-backend

  ports:
  - protocol: TCP
    port: 3000
    targetPort: 3000

  type: NodePort